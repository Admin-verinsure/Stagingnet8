@model DealEngine.WebUI.Models.Report.ReportViewModel

<div class="card">
    <div class="card-body">
        <h5 class="card-title">CG Data</h5>
        <p class="card-text" style="margin-top:30px;">Json</p>
        <div class="form-group">
            <select class="form-control form-select" Id="ProgrammeId" onchange="Serialise('29dfc24b-845d-4d4c-8d5c-ac46003d1e5a')">
                <option selected="selected" value="">Add New</option>
                <option  value="">change</option>
                @*@foreach (var client in Model.SelectedClients)
                {
                    <option value="@client.Value">@client.Text</option>
                }*@
            </select>            
        </div>
    </div>
</div>


@section pagespecific {

    <script type="text/javascript">

        function Serialise(exampleId) {
            var programmeId = document.getElementById('ProgrammeId').value;
            $.ajax({
                type: 'Post',
                url: '@Url.Action("Create", "Report")',
                data: { Id: exampleId }
            })
                .done(function (data) {
                    debugger;
                })
                .fail(function (jqXhr, textStatus, errorThrown) {
                    debugger;
                    console.log(errorThrown);
                });
        }
    </script>
}
