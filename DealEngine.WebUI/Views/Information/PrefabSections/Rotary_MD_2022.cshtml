@model DealEngine.WebUI.Models.InformationViewModel

<form id="TAViewModel" class="is-valid" data-accordianId="Cover Period and Product Selector">

    <label class="label">
        <strong>
            Your Requirements for Extra Covers:
            If you want a quote for covers for please select yes below:
        </strong>
    </label>
    <br />
    <br />

    <div class="form-group">
        <div class="row">

            <div class="col-9">
                <label class="label required">
                    Do you want cover or your Club or Club Trust assets (not real estate) ?
                </label>
            </div>
            <div class="col-3">
                <select id="HasClubTrustAssets" name="TAViewModel.HasClubTrustAssets" class="form-select" onchange="OnChange(this.id, this.name)" required>
                    @foreach (var option in Model.TAViewModel.HasClubTrustAssets)
                    {
                        <option selected="@option.Selected" value="@option.Value">@option.Text</option>
                    }
                </select>
            </div>
        </div>
    </div>


    <div class="form-group">
        <div class="row">
            <div class="col-9">
                <label class="label required">
                    Is any one Club or trust asset worth more than $15000?

                </label>
            </div>
            <div class="col-3">
                <select id="HasClubTrustAssetMore" name="TAViewModel.HasClubTrustAssetMore" class="form-select" onchange="OnChange(this.id, this.name)" required>
                    @foreach (var option in Model.TAViewModel.HasClubTrustAssetMore)
                    {
                        <option selected="@option.Selected" value="@option.Value">@option.Text</option>
                    }
                </select>
            </div>
        </div>
    </div>


    <div id="TAViewModel.HasClubTrustAssetMore" hidden>
        <div class="form-group">
            <div class="row">
                <div class="col-9">
                    <label for="Scheduledassets" class="label required">
                        Scheduled assets over $15000:
                    </label>
                </div>
                <div class="col-10">

                    <div class="container py-1">
                        <div class="row">
                            <div class="col-lg-12 mx-auto">
                                <div class="card rounded-0 border-0 shadow">
                                    <div class="card-body p-5">

                                        <div class="table-responsive">
                                            <table class="table" name="TAViewModel.ClubTrustAssetsInfo">
                                                <thead>
                                                    <tr>
                                                        <th scope="col">Description or Name</th>
                                                        <th scope="col">Current Value</th>
                                                        <th scope="col">Replacement Value</th>
                                                        <th scope="col">Owner</th>
                                                        <th scope="col"></th>
                                                    </tr>
                                                </thead>

                                                <tbody id="assettable">
                                                    @if (Model.TAViewModel.ClubTrustAssetsInfo != null)
                                                    {
                                                        int i = 1;

                                                        @foreach (var option in Model.TAViewModel.ClubTrustAssetsInfo)
                                                        {
                                                    <tr>
                                                        <td hidden>
                                                            <input class="form-control rounded-0" type="text" value="@option.Id">
                                                        </td>
                                                        <td>
                                                            <input class="form-control rounded-0" type="text" name='CTAViewModelDescriptionorName @i' placeholder="Description or name" value="@option.Name">
                                                        </td>
                                                        <td>
                                                            <input class="form-control rounded-0" name='CTAViewModelCurrentValue @i' min="15001" placeholder="Current Value" type="number" value="@option.CurrentVal">
                                                        </td>

                                                        <td>
                                                            <input class="form-control rounded-0" name='CTAViewModelReplacementValue @i' placeholder="Replacement Value" type="number" value="@option.ReplacementVal">
                                                        </td>
                                                        <td>
                                                            <select name='CTAViewModelOwner @i' class="form-select" required>
                                                                <option>Please select owner</option>

                                                                @foreach (var owner in Model.OrganisationViewModel.Organisations)
                                                                {
                                                                    if (owner.Id == option.Owner.Id)
                                                                    {
                                                                        <option selected="selected" value="@owner.Id">@owner.Name</option>

                                                                    }
                                                                    else
                                                                    {
                                                                        <option value="@owner.Id">@owner.Name</option>

                                                                    }
                                                                }

                                                            </select>
                                                            @*<input class="form-control rounded-0" type="text" name='CTAViewModelOwner @i' placeholder="Owner" value="@option.Owner">*@
                                                        </td>
                                                        <td><button class="btn btn-danger rounded-0" id="deleteRow" value="@option.Id "  onclick="deleteTrustAsset('@option.Id')"><i class="fa fa-trash"></i></button></td>
                                                    </tr>

                                                            i++;
                                                        }


                                                    }

                                                    <tr hidden>
                                                        <td>
                                                            <select id="organisationselection" class="form-select">
                                                                <option selected="selected" value="">Please select owner</option>

                                                                @foreach (var owner in Model.OrganisationViewModel.Organisations)
                                                                {
                                                                    <option value="@owner.Id">@owner.Name</option>
                                                                }
                                                            </select>
                                                        </td>
                                                    </tr>


                                                </tbody>
                                            </table>
                                        </div>
                                        <a class="btn btn-primary rounded-0 btn-block" onclick="AddNewRow()" id="insertRow">Add new row</a>
                                    </div>
                                </div>
                            </div>
                        </div>

                    </div>

                </div>
            </div>
        </div>
    </div>

</form>

