<?xml version="1.0" encoding="utf-8" ?>
<configuration>
  <nlog xmlns="http://www.nlog-project.org/schemas/NLog.xsd"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        autoReload="true"
        internalLogLevel="Info"
        internalLogFile="c:\temp\internal-nlog.txt">

    <!-- enable asp.net core layout renderers -->
    <extensions>
      <add assembly="NLog.Web.AspNetCore"/>
    </extensions>

    <!-- the targets to write to -->
    <targets>
		<!-- Create folder if missing -->
		<target xsi:type="File" name="debugFile"
				fileName="C:\Users\LENOVO\Downloads\logs\debug-${shortdate}.log"
				layout="${longdate} ${uppercase:${level}} ${logger} ${message}" />

		<target xsi:type="File" name="infoFile"
				fileName="C:\Users\LENOVO\Downloads\logs\info-${shortdate}.log"
				layout="${longdate} ${uppercase:${level}} ${logger} ${message}" />

		<target xsi:type="File" name="errorFile"
				fileName="C:\Users\LENOVO\Downloads\logs\error-${shortdate}.log"
				layout="${longdate} ${uppercase:${level}} ${logger} ${message}" />
	</targets>


	<!-- rules to map from logger name to target -->
	  <rules>
		  <logger name="TestLogger" minlevel="Debug" writeTo="debugFile" />

		  <!-- NHibernate SQL Logging -->
		  <logger name="NHibernate.Type" minlevel="Trace" writeTo="debugFile" />
		  <logger name="NHibernate.SQL" minlevel="Trace" writeTo="debugFile" />
		  <logger name="NHibernate.*" minlevel="Trace" writeTo="debugFile" />



		  <!-- Your existing generic rules -->
		  <logger name="*" minlevel="Debug" writeTo="debugFile" />
		  <logger name="*" minlevel="Info" writeTo="infoFile" />
		  <logger name="*" minlevel="Error" writeTo="errorFile" />

	  </rules>

  </nlog>
</configuration>